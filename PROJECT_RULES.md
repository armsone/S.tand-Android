# S.tand Android 프로젝트 개발 규칙

이 문서는 S.tand Android를 개발하면서 사람과 Codex가 공통으로 지켜야 할 기준이다. 사용자의 최신 직접 지시가 이 문서보다 우선하며, 규칙이 실제 프로젝트나 실행 환경과 맞지 않거나 불필요하거나 지킬 수 없으면 이유를 먼저 보고하고 사용자의 허용 범위에서 이 문서를 바로잡는다.

## 1. 공동 원칙과 작업 범위

- 불필요하거나 반복적인 확인을 줄여 주간 사용량과 크레딧을 아낀다. 같은 검증은 관련 상태가 바뀌었거나 이전 결과가 불충분할 때만 반복한다.
- 작업을 시작하기 전에 짧은 계획을 먼저 보여준다.
- 쓰기 작업은 이 저장소 루트 안에서만 수행한다. 사용자가 특정 외부 산출물 생성을 명시적으로 요청한 경우만 예외로 한다.
- iOS 원본 형제 저장소 `../S.tand`는 읽기 전용 기준이다. 파일을 수정·포맷·정리·커밋하지 않으며 기존 미커밋 변경도 그대로 보존한다.
- 사용자가 만든 변경과 현재 작업에 관계없는 변경을 임의로 되돌리거나 덮어쓰지 않는다.
- 요구사항이 불명확하면 기존 동작과 사용자 데이터를 보존하고, 최신 iOS의 확정 동작을 우선한다.
- 장시간 자율 작업의 중간 보고는 핵심 완료 항목과 실제로 확인 가능한 사용량만 간략히 쓰고 상세 변경은 최종 보고에 모은다.

## 2. 소스 수정과 검증

- 하나의 의미 있는 변경 묶음이 끝나면 먼저 컴파일 오류를 확인하고, 오류가 있으면 새 기능보다 우선해 해결한다.
- 완료 전에는 Gradle wrapper로 `assembleDebug`와 로컬 JVM 단위 테스트를 실행한다. 가능한 경우 lint도 확인한다.
- 문서만 수정한 경우 앱 빌드와 설치를 생략할 수 있다.
- 화면 수정은 컴파일만으로 완료 처리하지 않는다. 버튼 표시, 터치 범위, 상태 전환, 세로·가로·600dp 이상 화면처럼 실제 실행 확인이 필요한 변경에 한해 허가된 에뮬레이터나 실기기에서 확인하고, 실행 검증이 불필요하거나 이미 같은 상태를 확인했다면 반복하지 않는다.
- 연결 기기나 에뮬레이터가 다른 작업에 사용 중이면 `adb`, 설치, 실행, 계측 테스트, 권한·화면 설정 변경을 하지 않는다. 소스 변환·로컬 빌드·JVM 테스트만 진행하고 실기기 검증을 보류했다고 보고한다.
- 설치·실행 검증은 사용자가 이 프로젝트에 할당한 전용 기기 또는 전용 AVD에서만 수행한다. 개인 기기의 앱 삭제나 데이터 초기화는 사전 승인 없이 하지 않는다.
- Android 에뮬레이터/AVD는 정기적·관행적으로 실행하지 않는다. 사용자가 요청했거나 UI 배치, OS 권한·생명주기, 설치 흐름, 화면 크기별 동작처럼 빌드·lint·JVM 테스트만으로 확인할 수 없는 사항이 있을 때만 사용한다.
- 카메라·플래시·마이크·센서·공유·위젯처럼 JVM이나 일반 에뮬레이터만으로 충분하지 않은 기능은 실기기 사용이 허용된 뒤 별도 검증한다.
- 제한된 실행 환경에서 Gradle native service, Android SDK, `adb` 접근 오류가 나면 개발 도구 고장으로 단정하지 않는다. 샌드박스 밖 실행 승인이 필요한지 먼저 구분하고, 같은 읽기 전용 확인을 허용된 환경에서 재시도한 뒤 복구를 안내한다.

## 2-1. 하루의 첫 작업 시작 절차

- 한국 시간 기준 그날의 첫 Android 작업을 시작하면, 먼저 `docs/IOS_DESIGN_HANDOFF.md`와 iOS 형제 저장소 `../S.tand`가 전달한 최신 디자인 레시피·기준 커밋·변경 파일을 읽기 전용으로 확인한다.
- 디자인 레시피를 Android 기능, 화면, 제스처, 상태 저장, 테스트 및 실기기 확인 항목으로 나눈 작업 목록을 만든다.
- 작업 목록별 난이도와 검증 범위를 근거로 이번 작업이 소비할 것으로 예상되는 주간 사용량을 퍼센트포인트 범위로 제시한다. 이 값은 계획용 추정치이며 실제 측정값처럼 표현하지 않는다.
- 사용자가 현재 주간 사용량이나 이번 작업에 허용할 사용량을 알려 주면 그 범위에 맞춰 우선순위와 작업 묶음을 조정한 뒤 구현을 시작한다.
- 사용량 확인 전에는 디자인 레시피 확인, 읽기 전용 조사와 작업 목록 작성까지만 수행하고 대규모 구현은 시작하지 않는다.

## 2-2. `마무리 하자` 절차

- 사용자가 `마무리 하자` 또는 같은 의미로 지시하면 새 기능을 시작하지 않고 현재 변경을 하나의 배포 후보로 정리한다.
- 같은 검증을 반복하지 않고 코드 오류 확인을 한 번 수행한다. 기본 검증은 Gradle wrapper의 `assembleDebug`, 로컬 JVM 단위 테스트와 lint이며, 실패하면 오류를 고친 뒤 실패한 검증만 다시 확인한다.
- 검증된 APK를 현재 지정된 Android 기기에 데이터 유지 방식으로 설치하고 실행 및 versionCode를 확인한다. 현재 기본 설치 대상은 `SM-T500`과 `SM-F968N`이며 사용자의 최신 기기 지시가 우선한다.
- 검증한 설치본과 동일한 APK를 바탕화면에 `S.tand-월일-시간.apk` 형식으로 만들고 체크섬 일치를 확인한다.
- 현재 작업과 관련된 파일만 커밋하고 `main`을 GitHub 원격 `origin`에 푸시한다. 비밀정보, `local.properties`, APK, 서명 키와 빌드 산출물은 커밋하지 않는다.
- 설치, 배포 파일, 커밋 또는 푸시가 실패하면 성공했다고 보고하지 않고 원인과 남은 조치를 명확히 알린다.

## 3. 버전 관리

- 앱의 `versionName`은 `0.0.1`에서 시작하며 별도 지시 전까지 유지한다.
- Android의 `versionCode`는 Play·설치 규칙에 맞는 양의 단조 증가 정수로 관리한다. `0`은 사용하지 않고 현재 기준은 `28`이다.
- 사용자에게 보이는 별도 빌드 표기도 `0.0.1`에서 시작한다. 소규모 배포 후보는 마지막 자리를, 큰 배포 후보는 가운데 자리를 올린다.
- 컴파일 확인과 로컬 단위 테스트를 실행할 때마다 버전을 올리지 않는다. 설치·배포할 검증 후보를 새로 만들 때만 `versionCode`와 빌드 표기를 함께 올린다.
- AI 버전은 실제 앱 기능이나 정보 화면에서 사용될 때만 별도로 관리하며, 존재하지 않는 값을 임의로 만들지 않는다.

## 4. 커밋과 푸시

- 이 폴더에 Git 저장소와 원격이 구성되어 있지 않으면 임의로 초기화하거나 원격을 추측하지 않는다. 이 경우 커밋·푸시 불가 상태를 완료 보고에 명시한다.
- Git과 원격이 사용 가능해진 뒤에는 도구 호출 횟수나 파일 저장 횟수가 아니라, 컴파일·테스트 가능한 의미 있는 변경 묶음을 누적한다.
- 검증된 변경 묶음 10개가 쌓이거나 큰 기능 하나가 완료되면 현재 작업과 관련된 파일만 의도적으로 커밋한다. 푸시는 사용자가 지정한 원격과 브랜치가 확인된 경우에만 수행한다.
- 푸시 전에 현재 브랜치, 원격 변경, 충돌 가능성을 확인한다. 사용자 변경을 우선 보존하며 관련 없는 파일을 커밋하지 않는다.
- 강제 푸시는 사용자의 명시적 승인과 원격 변경 확인 없이 수행하지 않는다.
- GitHub 원터치 업데이트 배포는 `docs/GITHUB_UPDATES.md`를 따른다. 소스 푸시만으로 업데이트가 배포됐다고 보고하지 않으며, 같은 개발용 서명의 검증된 APK를 정확한 태그와 파일명으로 GitHub Release에 올리고 공개 조회까지 확인해야 한다.

## 5. 재현 가능한 Android 빌드

- 다른 개발자가 clone한 뒤 별도의 소스 수정 없이 Android Studio와 저장소의 Gradle wrapper로 빌드할 수 있어야 한다.
- 빌드에 필요한 Gradle 설정, Manifest 권한, 리소스, 폰트, 라이선스, 테스트를 저장소에 포함한다.
- 개인 Mac의 절대 경로를 추적 파일에 넣지 않는다. `local.properties`, SDK 경로, 서명 키처럼 사용자별 값은 버전 관리에서 제외한다.
- 최소·대상 SDK와 필요한 JDK·Android Studio 조건은 README에 기록한다.
- 계정, 광고, 분석 SDK, 자체 백엔드, 클라우드 업로드는 사용자 결정 없이 추가하지 않는다. 녹음과 설정은 기기 안에 보관하고 Android 백업에서도 제외한다.

## 6. iOS 동등성과 Android 설계

- 매일 첫 Android 작업에서 iOS 기준 커밋과 전날 이후 변경 파일을 읽기 전용으로 한 번 확인한다. 하나의 작업 묶음이 이미 고정 커밋을 기준으로 시작됐다면 새 iOS 변경은 다음 묶음에 반영한다.
- iOS 비교는 기능과 수치뿐 아니라 모든 화면의 텍스트, 항목별 아이콘, 버튼 동작, 페이지 존재 여부, 배치·간격·크기·색상·상태 디자인을 함께 대조한다.
- iOS의 텍스트·아이콘·핵심 동작은 정확히 맞추고, Android의 넓은 화면·성능·접근성 장점은 기능 의미를 바꾸지 않는 범위에서 개선한다.
- iOS에서 아직 안정되지 않은 기능을 Android에서 추측해 먼저 만들지 않는다. 확정되지 않았거나 즉시 이식할 수 없는 항목은 누락시키지 말고 `docs/IOS_PARITY.md`에 차이, 이유, 후속 작업을 기록한다.
- 사용자에게 보이는 핵심 상태명은 `오브제 모드`, `매이트 모드`, `화들짝 모드`로 통일한다.
- 카드, 버튼, 입력창, 팝업, 플로팅 패널은 둥근 사각형을 기본으로 한다. 전체 화면 루트, 원본 미디어, 격자·구분선처럼 기능상 필요한 경우만 예외로 한다.
- 시계·날씨·배터리·하단 기능 버튼처럼 조명 위에 놓이는 패널은 iOS `FlipPanelSurface`를 기준으로 따뜻한 저투명 흰색 세로 그라데이션과 매우 약한 흰색 테두리를 공통 사용한다. 강한 블러나 회색 불투명판을 기본값으로 쓰지 않으며, 모달·입력창처럼 가독성 보호가 더 중요한 화면만 더 불투명하게 할 수 있다.
- 바형 휴대폰을 기본으로 하되 600dp 이상에서는 단순 확대하지 않고 열 수를 늘리거나 편집·미리보기를 나눈다. 접힘·펼침과 회전 중에도 선택 상태와 편집 내용을 보존한다.
- 화면 방향 고정은 Android 대화면 정책상 무시될 수 있으므로 적응형 레이아웃을 항상 함께 제공한다.

## 7. 실기기와 외부 APK

- 실기기 작업이 허용되면 먼저 대상 기기의 serial과 다른 작업의 사용 여부를 확인한다. 여러 기기가 있으면 `ANDROID_SERIAL` 등으로 대상을 명시해 오설치를 막는다.
- Mac과 할당된 휴대폰이 `zangzip` 네트워크에 연결돼 있으면 무선 디버깅을 우선하고, 불가능할 때만 USB를 사용한다.
- 전용 실기기 검증 동안만 화면 자동 꺼짐과 충전 중 항상 켜짐을 조정하고, 작업 후 원래 상태로 복원한다. 잠긴 기기의 자격 증명을 추측하거나 입력하지 않는다.
- 사용자가 같은 화면 제목의 참고 이미지를 다시 주면 최신 상태나 추가 상태의 기준으로 보고 Android 디자인 비교에 반영한다.
- 바탕화면 APK 생성은 사용자가 명시적으로 요청한 경우에만 수행한다. 이때도 삭제 대상은 정확히 확인한 기존 `S.tand-Android-*.apk` 산출물로 한정하고 최신 검증본 하나만 남긴다.

## 8. 기능 사전과 동등성 문서

- 새 기능을 만들거나 사용자에게 보이는 명칭·핵심 동작을 바꾸면 `docs/IOS_PARITY.md`의 기능 사전과 동등성 체크리스트를 같은 작업에서 갱신한다.
- 기능 사전에는 화면 명칭, 목적, 주요 선택지, 중요한 동작 기준을 짧고 명확하게 기록한다.
- 기능이 없어지거나 명칭이 바뀌면 오래된 설명도 함께 수정하거나 제거한다.
- 앱 안에 별도의 카피라이터 정보창이 실제로 추가되기 전에는 존재하지 않는 화면을 만들었다고 가정하지 않는다.

## 9. `N%로 작업 시작해` 단축 지시

- 사용자가 `N%로 작업 시작해`라고 말하면 `N`을 이번 작업에 허용한 주간 사용량 퍼센트포인트 상한으로 해석하고, 시작 시점의 iOS 커밋을 고정 기준으로 사용한다.
- Codex 또는 CCMB의 현재 주간 사용량·남은 사용량·크레딧을 요청받으면 별도의 임시 app-server 클라이언트나 웹 검색을 사용하지 말고 `/Users/armsone/.codex/bin/ccmb-usage`를 먼저 실행한다.
- 명령의 JSON에서 `weeklyRemainingPercent`를 남은 주간 사용량으로, `creditBalance`를 남은 크레딧으로 보고한다. `creditBalance`가 `null`이면 추정하지 않고 현재 응답에 크레딧 잔액 정보가 없다고 알린다.
- `origin`과 `fresh`를 함께 확인한다. `fresh`가 `true`이면 최신 데이터로 간단히 답하고, `false`이면 `ageSeconds`와 함께 오래된 데이터임을 명확히 알린다.
- 공용 명령이 실패하면 반복해서 오래 기다리거나 다른 출처의 수치로 대체하지 말고 오류 원인을 간결하게 보고한다.
- 로그인 토큰, 세션 파일, 인증정보와 명령의 불필요한 원시 응답은 출력하거나 수정하지 않는다.
- 정확한 시작값을 확인할 수 없는데 N% 상한 준수가 필수라면 사용자에게 시작·종료 값을 제공해 달라고 요청한다. 사용자가 값 없이 계속하라고 하면 합의된 기능 범위의 안전한 한 묶음까지만 진행하고 정확한 퍼센트 사용을 주장하지 않는다.
- 사용량을 확인할 수 있는 경우 의미 있는 작업 묶음 사이에만 다시 확인한다. 상한에 도달하면 새 작업을 시작하지 않고 진행 중인 묶음의 컴파일 오류 해결과 안전한 정리까지만 수행한다.
- 사용자가 `이제 끝내`라고 말하면 새 묶음을 시작하지 않고 현재 묶음의 소스 정리, 컴파일 오류 해결, 필요한 검증까지만 마친 뒤 보고한다.
- 사용량을 확인할 수 있을 때만 중간 보고에 `허용 N%, 사용 U%` 형식을 쓴다. 지연과 반올림으로 정확히 N%에서 멈추지 못할 수 있음을 최종 보고에 밝힌다.
- iOS 동등성은 기능, 동작 수치, 데이터 구조, 디자인의 검증 가능한 체크리스트로 계산한다. 직접 확인하지 못한 항목은 달성으로 계산하지 않는다.
- 마무리할 때 확인 가능한 사용량, iOS 동등성 달성률, 남은 기능·디자인 작업과 예상 소요를 보고한다.

## 10. 완료 보고

- 수정한 핵심 내용, 로컬 빌드·단위 테스트 결과, 설치·실행 여부를 간단히 보고한다.
- 기기 충돌이나 권한 때문에 설치·실행을 보류했다면 그 사실과 후속 검증 항목을 명확히 적는다.
- 커밋·푸시를 하지 않았거나 현재 저장소 구조상 할 수 없으면 그 상태를 명확히 알린다.
- 앱 삭제, 데이터 초기화, 외부 APK 교체, 강제 푸시처럼 복구가 어려운 작업은 실행 전 대상을 확인하고 실행 후 복구 가능 여부를 보고한다.
